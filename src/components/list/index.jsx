import React from 'react';
import PropTypes from 'prop-types';

const List = (props) => {
  return(
  	<div>List</div>
  );
};

List.propTypes = {
  props: PropTypes.object.isRequired
};

export default List;