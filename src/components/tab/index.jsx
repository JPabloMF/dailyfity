import React from 'react';
import PropTypes from 'prop-types';

const Tab = (props) => {
  return(
  	<div>Tab</div>
  );
};

Tab.propTypes = {
  props: PropTypes.object.isRequired
};

export default Tab;