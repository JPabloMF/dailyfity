import React from 'react';
import PropTypes from 'prop-types';

const Logo = (props) => {
  return(
  	<div>Logo</div>
  );
};

Logo.propTypes = {
  props: PropTypes.object.isRequired
};

export default Logo;