import React from 'react';
import PropTypes from 'prop-types';

const WeekRoutine = (props) => {
  return(
  	<div>WeekRoutine</div>
  );
};

WeekRoutine.propTypes = {
  props: PropTypes.object.isRequired
};

export default WeekRoutine;